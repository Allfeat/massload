[package]
name = "allfeat-core"
version.workspace = true
edition.workspace = true
description = "Core models and validation for Allfeat ecosystem - MIDDS structures"
license.workspace = true
repository.workspace = true
homepage.workspace = true
authors.workspace = true
keywords = ["allfeat", "midds", "music", "metadata", "blockchain"]

[features]
default = ["validation"]
validation = ["jsonschema", "regex", "once_cell"]

[dependencies]
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true

# Optional dependencies (for backend validation only)
jsonschema = { workspace = true, optional = true }
regex = { workspace = true, optional = true }
once_cell = { workspace = true, optional = true }

[dev-dependencies]

