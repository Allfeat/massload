apiVersion: v1
kind: Service
metadata:
  name: massload-backend
  namespace: massload
  labels:
    app.kubernetes.io/name: massload
    app.kubernetes.io/component: backend
spec:
  type: ClusterIP
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: massload
    app.kubernetes.io/component: backend

