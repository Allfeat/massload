apiVersion: v1
kind: ConfigMap
metadata:
  name: massload-config
  namespace: massload
  labels:
    app.kubernetes.io/name: massload
data:
  # Backend config
  RUST_LOG: "info"
  # Frontend runtime config (override in overlays for each environment)
  BACKEND_URL: "https://api.massload.allfeat.io"
  BLOCKCHAIN_RPC: "wss://node.allfeat.io"

