# IMPORTANT: This is a template. Never commit real secrets!
# Use: kubectl create secret generic massload-secrets --from-literal=ANTHROPIC_API_KEY=sk-ant-xxx -n massload
apiVersion: v1
kind: Secret
metadata:
  name: massload-secrets
  namespace: massload
  labels:
    app.kubernetes.io/name: massload
type: Opaque
stringData:
  ANTHROPIC_API_KEY: "REPLACE_ME"

